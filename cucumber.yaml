---
default: --no-source --color --format pretty -p retry -p html --publish-quiet  # or --format progress

ci: -p json --format pretty # or --format progress
html: --format html --out=log/reports.html
json: --format json --out=log/reports.json
retry: --retry 1